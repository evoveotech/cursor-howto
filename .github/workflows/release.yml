name: Build and Release EPUB

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Build EPUB
        run: uv run --project scripts --frozen scripts/build_epub.py

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: cursor-howto-guide.epub
          generate_release_notes: true
